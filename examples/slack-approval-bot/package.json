{
  "name": "slack-approval-bot",
  "version": "0.0.0",
  "private": true,
  "description": "Slack-bridged approval flow. Agent posts a draft to a Slack channel; reactions (👍 / ✏️ / ❌) webhook back to the framework, mapped to approved / manually_edited / rejected.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-feedback-middleware/core": "workspace:*",
    "@ai-feedback-middleware/in-memory": "workspace:*",
    "@ai-feedback-middleware/reference": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
